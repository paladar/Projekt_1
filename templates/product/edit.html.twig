{% extends 'base.html.twig' %}

{% block title %}{{'product.edit'|trans}} #{{product.id}}{% endblock %}

{% block main %}
    <div class="row">
        <h1 class="col">{{'product.edit'|trans}} #{{product.id}}</h1>
    </div>
    <div class="row buttons">
        <div class="col">
            <a class="btn btn-primary" href="{{ path('homepage') }}">{{'back'|trans}}</a>
        </div>
        <div class="col text-right">
            {{ include('product/_delete_form.html.twig') }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
</div>
{% endblock %}