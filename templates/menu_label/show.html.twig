{% extends 'base.html.twig' %}

{% block title %}MenuLabel{% endblock %}

{% block main %}
    <h1>MenuLabel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_label.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ menu_label.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('homepage') }}">back to list</a>

    <a href="{{ path('menu_label_edit', {'id': menu_label.id}) }}">edit</a>

    {{ include('menu_label/_delete_form.html.twig') }}
{% endblock %}